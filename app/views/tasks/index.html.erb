<h1>My tasks</h1>
 <ul>
  <% @tasks.each do |task| %>
   <li>
    <%=check_box("tag", tag.id, {checked:task.completed}) %> <%=link_to task.title, task_path(task) %>
   </li>
   <% end %>
 </ul>
<%=link_to "Add a new task", new_path(:new) %>
